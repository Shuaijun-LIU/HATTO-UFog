"""Framework ↔ Isaac Sim integration layer (planning + replay).

Design rule: all integration logic lives under `Isaac/framework_integration/` and
must not change the default behavior of existing `Isaac/scripts/*.py`.
"""

